
<div class="container mt-5">

    @model List<CategoryFormViewModel> 

<div class="container mt-5">

    <div>

        <div class="float-start"> <h2>Kategoriler</h2>  </div>
        <div class="float-end"> <a asp-area="Admin" asp-controller="Category" asp-action="New" class="btn btn-success"><i class="bi bi-plus-square"></i> Yeni Kategori</a>   </div>

    </div>


    <div style="padding-top:75px">

        <table class="table table-striped">
            <tr>
                <th>Kategori Adı</th>
                <th>Açıklaması</th>
                <th></th>
            </tr>

                @foreach (var category in Model)
                {
                    <tr>
                        <td>@category.Name</td>
                        <td>@category.Description</td>
                        <td>
                            <a asp-area="Admin" asp-controller="Category" asp-action="Edit" asp-route-id="@category.Id" class="btn btn-sm btn-primary"><i class="bi bi-pencil-square"></i> Düzenle</a>
                            <a class="btn btn-sm btn-danger"><i class="bi bi-trash3"></i> Sil</a>
                        </td>
                    </tr>
                }
        </table>

    </div>


</div>